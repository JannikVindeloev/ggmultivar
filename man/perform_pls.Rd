% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pls.R
\name{perform_pls}
\alias{perform_pls}
\title{Perform Partial Least Squares Regression}
\usage{
perform_pls(
  X,
  Y,
  n_components = 2,
  scale = TRUE,
  center = TRUE,
  sample_names = NULL,
  x_variable_names = NULL,
  y_variable_names = NULL
)
}
\arguments{
\item{X}{A numeric data frame or matrix of predictors. Rows are observations, columns are variables.}

\item{Y}{A numeric data frame or matrix of responses. Rows are observations, columns are response variables.}

\item{n_components}{Number of PLS components to compute. Default is 2.}

\item{scale}{Logical, whether to scale variables to unit variance. Default is TRUE.}

\item{center}{Logical, whether to center variables by their mean. Default is TRUE.}

\item{sample_names}{Optional character vector of sample names. If NULL, defaults to Sample_1, Sample_2, etc.}

\item{x_variable_names}{Optional character vector of X variable names. If NULL, uses column names from X.}

\item{y_variable_names}{Optional character vector of Y variable names. If NULL, uses column names from Y.}
}
\value{
A list containing:
\itemize{
\item scores: Tidy data frame with PLS scores for each sample
\item loadings: Tidy data frame with PLS loadings for each X variable
\item y_loadings: Tidy data frame with PLS loadings for each Y variable
\item weights: Tidy data frame with PLS weights for each X variable
\item explained_variance: Data frame with variance explained by each component
\item regression_coefficients: Matrix of regression coefficients
\item r2: R-squared values for each component
}
}
\description{
Perform PLS regression on predictor and response matrices and return results in tidy format.
}
\examples{
# Example with simulated data
set.seed(123)
X <- matrix(rnorm(50 * 10), nrow = 50, ncol = 10)
Y <- matrix(rnorm(50 * 2), nrow = 50, ncol = 2)
pls_result <- perform_pls(X, Y, n_components = 3)
head(pls_result$scores)
head(pls_result$loadings)

}
