% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca.R
\name{perform_pca}
\alias{perform_pca}
\title{Perform Principal Component Analysis}
\usage{
perform_pca(
  data,
  n_components = 2,
  scale = TRUE,
  center = TRUE,
  sample_names = NULL,
  variable_names = NULL
)
}
\arguments{
\item{data}{A numeric data frame or matrix. Rows are observations, columns are variables.}

\item{n_components}{Number of principal components to compute. Default is 2.}

\item{scale}{Logical, whether to scale variables to unit variance. Default is TRUE.}

\item{center}{Logical, whether to center variables by their mean. Default is TRUE.}

\item{sample_names}{Optional character vector of sample names. If NULL, defaults to Sample_1, Sample_2, etc.}

\item{variable_names}{Optional character vector of variable names. If NULL, uses column names from data.}
}
\value{
A list containing:
\itemize{
\item scores: Tidy data frame with principal component scores for each sample
\item loadings: Tidy data frame with principal component loadings for each variable
\item explained_variance: Data frame with variance explained by each component
\item sdev: Vector of standard deviations of the principal components
\item rotation: Matrix of variable loadings (rotation matrix)
}
}
\description{
Perform PCA on a data matrix and return results in tidy format.
}
\examples{
data(mtcars)
# Use only numeric columns
pca_result <- perform_pca(mtcars[, 1:7], n_components = 3)
head(pca_result$scores)
head(pca_result$loadings)
pca_result$explained_variance

}
